language: node_js
node_js:
  - 'lts/*'

cache:
  directories:
    - 'node_modules'

install:
  - npm install

script:
  - npm test

deploy:
  provider: heroku
  api_key: $HEROKU_AUTH_TOKEN
  app: react-picture-copy
  skip_cleanup: true
